/** @format */

import {styled} from 'stitches.config'

export const AppWrapper = styled('div', {
 display: 'flex',
 flexDirection: 'column',
 minHeight: '100vh',
 position: 'relative',
 zIndex: 0
})
